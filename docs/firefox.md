# Extensions:
- Dark Reader
- Auto Tab Discard
  - # set discard if more than 20 active tabs)
- Decentraleyes
- Cookie Auto Delete
- Zoom Page WE
  - # set to 133 on 4k-monitor
## Tridactyl
useful settings
```
bind . fillcmdline find
bind ? fillcmdline find -1
bind n findnext
bind N findnext -1
bind \<space> nohlsearch
bind j scrollline 3
bind k scrollline -3
set theme dark
viewconfig [nmaps] # to show config
viewconfig --user # only changed settings
```
